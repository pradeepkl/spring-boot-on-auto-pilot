spring:
  datasource:
    url: jdbc:h2:mem:ekarttestdb
    username: sa
    password: ''
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

ekart:
  dev:
    seeder:
      enabled: false

logging:
  level:
    root: WARN
